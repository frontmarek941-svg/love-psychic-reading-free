<!DOCTYPE html>
<html <?php language_attributes(); ?>>
<head>
    <meta charset="<?php bloginfo( 'charset' ); ?>">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <?php wp_head(); ?>
</head>
<body <?php body_class(); ?>>
<?php wp_body_open(); ?>

<nav class="site-nav" role="navigation" aria-label="<?php esc_attr_e( 'Main Navigation', 'lprf-theme' ); ?>">
    <div class="nav-inner">
        <!-- Logo -->
        <a href="<?php echo esc_url( home_url( '/' ) ); ?>" class="nav-logo">
            <?php echo lprf_icon( 'heart', 'icon-lg' ); ?>
            <span><?php bloginfo( 'name' ); ?></span>
        </a>

        <!-- Desktop Nav -->
        <div class="nav-links">
            <?php
            if ( has_nav_menu( 'primary' ) ) {
                wp_nav_menu( [
                    'theme_location' => 'primary',
                    'container'      => false,
                    'items_wrap'     => '%3$s',
                    'depth'          => 1,
                ] );
            } else {
                lprf_fallback_menu();
            }
            ?>
        </div>

        <!-- Desktop CTA -->
        <div class="nav-cta">
            <a href="<?php echo esc_url( home_url( '/free-love-reading/' ) ); ?>" class="btn btn-primary" style="padding:10px 24px;font-size:0.875rem;">
                <?php esc_html_e( 'Get Your Free Reading', 'lprf-theme' ); ?>
            </a>
        </div>

        <!-- Mobile Toggle -->
        <button class="mobile-toggle" aria-label="<?php esc_attr_e( 'Toggle menu', 'lprf-theme' ); ?>">
            <span class="icon-open"><?php echo lprf_icon( 'menu', 'icon-lg' ); ?></span>
            <span class="icon-close" style="display:none;"><?php echo lprf_icon( 'x', 'icon-lg' ); ?></span>
        </button>
    </div>

    <!-- Mobile Menu -->
    <div class="mobile-menu">
        <?php
        if ( has_nav_menu( 'primary' ) ) {
            wp_nav_menu( [
                'theme_location' => 'primary',
                'container'      => false,
                'items_wrap'     => '%3$s',
                'depth'          => 1,
            ] );
        } else {
            lprf_fallback_menu();
        }
        ?>
        <a href="<?php echo esc_url( home_url( '/free-love-reading/' ) ); ?>" class="btn btn-primary" style="display:block;text-align:center;margin-top:12px;">
            <?php esc_html_e( 'Get Your Free Reading', 'lprf-theme' ); ?>
        </a>
    </div>
</nav>

<main>
